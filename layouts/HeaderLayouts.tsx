import React from 'react'

const HeaderLayouts = ({ children }: any) => {
    return (
        <React.Fragment>
            <div className="px-[88px]">{children}</div>
        </React.Fragment>
    )
}

export default HeaderLayouts